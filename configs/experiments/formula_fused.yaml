run_name: "formula_fused_sq"
index_type: "formula_fused"

# Retrieval parameters
num_hits: 1000

# Paths — leave null to use defaults:
#   formula_index_path    → data/indices/formula/fused_sq
#   formula_embedding_dir → data/formula-indexing
#   formula_tsv_base_dir  → data/raw/collection/formula
formula_index_path: null
formula_embedding_dir: null
formula_tsv_base_dir: null

# Optional
index_corpus_limit: null
force_rebuild: false
experiment_name: "Formula Index Runs"
